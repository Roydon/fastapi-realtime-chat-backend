# Template only - do not commit real values. Create with `kubectl create secret generic`
# or (recommended) manage via External Secrets / Sealed Secrets / your cloud's secret store.
apiVersion: v1
kind: Secret
metadata:
  name: chat-api-secrets
  namespace: chat-backend
type: Opaque
stringData:
  JWT_SECRET: "replace-me-32-chars-minimum"
  POSTGRES_PASSWORD: "replace-me"
  S3_ACCESS_KEY: "replace-me"
  S3_SECRET_KEY: "replace-me"
